open Core
module CodeGen (Tree : ast) = struct


  type t 

  let t


  let traverseTree (tree:ast) (acc:string) : string =
    (*TODO - Match the Tree For DFS*)

    match tree with 
    | Empty -> acc
  
    

  let converTreeToText (tree:ast): string = 



end
